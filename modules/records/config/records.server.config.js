'use strict';

/**
 * Module dependencies
 */
const path = require('path'),
  config = require(path.resolve('./config/config'));

/**
 * Singlechoices module init function.
 */
module.exports = function (app, db) {

};
